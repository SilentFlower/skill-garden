diff --git a/.codex/agents/trellis-implement.toml b/.codex/agents/trellis-implement.toml
--- a/.codex/agents/trellis-implement.toml
+++ b/.codex/agents/trellis-implement.toml
@@ -5,6 +5,10 @@
 # model_reasoning_effort = "high"
 
 developer_instructions = """
+<!-- BEGIN skill-garden patch codex-agents-untracked-context v0.6 -->
+
+If the dispatch prompt's first line is `Untracked work: <work-id>`, run `python3 ./.trellis/scripts/untracked_flow.py status --verbose`, require the same work id, use its summary/stage plus the actual diff, relevant specs, validation context, and responsibility supplied by the main agent, and skip any task lookup or task-artifact loading below. The helper is only a workflow cursor; do not require or invent a task path, JSONL file, scope, baseline, fingerprint, or owner evidence. Otherwise continue with the agent's normal task context rules.
+<!-- END skill-garden patch codex-agents-untracked-context v0.6 -->
 You are running as the `trellis-implement` sub-agent. The main session has dispatched you to do the work.
 
 CRITICAL — Recursion guard (read first):
