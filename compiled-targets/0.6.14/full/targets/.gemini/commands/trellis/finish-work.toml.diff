diff --git a/.gemini/commands/trellis/finish-work.toml b/.gemini/commands/trellis/finish-work.toml
--- a/.gemini/commands/trellis/finish-work.toml
+++ b/.gemini/commands/trellis/finish-work.toml
@@ -2,6 +2,11 @@
 
 prompt = """
 # Finish Work
+<!-- BEGIN skill-garden patch trellis-finish-work-gemini-owner v0.6 -->
+## Flower Workflow Owner
+
+Load and follow the platform-native `trellis-finish-work` skill as the sole owner of this action. Do not execute the legacy steps below after that skill has been loaded.
+<!-- END skill-garden patch trellis-finish-work-gemini-owner v0.6 -->
 
 Wrap up the current session: archive the active task (and any other completed-but-unarchived tasks the user wants to clean up) and record the session journal. Code commits are NOT done here — those happen in workflow Phase 3.4 before you invoke this command.
 
